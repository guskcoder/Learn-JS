// O que é um array
// index
const nomes = ['Maria', 'João', 'Felipe', 'Isabel'];

// Selecionar um item por index
// console.log(nomes[1]);

// Saber a quantidade de itens
// console.log(nomes.length);

// Adicionar algum item na array
// nomes.push('Gustavo');

// splice escolhe apartir de qual deve remover
// nomes.splice(1, 2);

// Slice

const novoArray = nomes.slice(1, -1);

console.log(novoArray);
